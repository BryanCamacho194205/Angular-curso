import { ProductsOrderedPipe } from './products-ordered.pipe';

describe('ProductsOrderedPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsOrderedPipe();
    expect(pipe).toBeTruthy();
  });
});
